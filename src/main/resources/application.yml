spring:
  application:
    name: planit

# 데이터/인증은 Firebase(Authentication / Firestore / Storage)로 이전됨.
# 연동은 프론트엔드(frontend/, React)에서 Firebase SDK로 직접 처리한다.
# 이 Spring 앱은 정적 리소스 서빙만 담당하므로 DB(datasource/JPA) 설정이 없다.

server:
  port: 8080
